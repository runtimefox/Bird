class DASHBOARD_PAGES {
  private root = '/dashboard';
  HOME = `${this.root}/home`;
  PROFILE = `${this.root}/profile`;
  SETTINGS = `${this.root}/settings`;
  NOTIFICATION = `${this.root}/notifications`;
}
export const DASHBOARD = new DASHBOARD_PAGES();
